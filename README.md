# Deadhead Finder Sandbox v3.0.1

Hotfix for the first v3.0 test.

- Restores a clearly visible **Manual GTFS upload** control at all times.
- Uses a real button ("Choose GTFS zip") instead of relying on the browser's native file-input rendering.
- Manual upload stays available even after an automatic GTFS load succeeds.
- Demo mode now explicitly explains that only Babylon can calculate from the built-in sample; the other saved deadhead locations require the full GTFS.
- No Crew Book or routing logic was changed from v3.0.
